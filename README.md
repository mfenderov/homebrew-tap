# 🍺 Homebrew Tap

Homebrew formulae for my tools.

## Installation

```bash
brew tap mfenderov/tap
```

## Available Formulae

| Formula | Description |
|---------|-------------|
| `claude-sync` | Beautiful CLI tool for syncing Claude Code configurations across machines |

## Usage

```bash
# Install claude-sync
brew install mfenderov/tap/claude-sync

# Or directly without adding the tap
brew install mfenderov/tap/claude-sync
```

## Updating

```bash
brew update
brew upgrade claude-sync
```

